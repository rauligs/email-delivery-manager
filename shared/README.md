# Shared

Python package for resources shared by the API and background apps, such as
database models, domain models, and common validation helpers.

```sh
uv sync
uv run pytest
uv run ruff check .
uv run ruff format --check .
```
