# IDE
.idea/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
pytest-of-*/
# Node / frontend dependencies and build artifacts
node_modules/
.next/
out/
.turbo/
playwright-report/
test-results/
.playwright/
uv-*.lock
# Local env (non-secret operator config only; never commit secrets)
.env

# ai-workflow installer backups (setup-ai-workflow.sh)
*.bak.2*

# Lambda deploy build workspace (leaks only if the deploy CLI is killed mid-run)
notification-engine-deploy-*/
