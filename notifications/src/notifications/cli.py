"""Console-script entrypoint.

Real commands (deploy, tenant setup, smoke test) are added in later issues.
This stub keeps the `notifications` console script wired up from issue 001.
"""

import sys


def main() -> int:
    print("notifications CLI placeholder — commands are added in later issues")
    return 0


if __name__ == "__main__":
    sys.exit(main())
